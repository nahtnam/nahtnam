name: setup
description: Install the toolchain and dependencies

runs:
  using: composite
  steps:
    - uses: jdx/mise-action@v4
      with:
        version: "2026.9.1"
    - run: bun ci
      shell: bash
